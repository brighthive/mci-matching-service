version: '3.5'
services:
    mci:
        image: brighthive/mci-matching-service:1.0.0
        ports:
            - "9000:8000"
        environment:
            # How should this work with multiple environments?
            - APP_ENV=DEVELOPMENT
        networks:
            - mci_network
networks:
    mci_network:
        external: true